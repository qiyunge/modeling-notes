# Algorithm Design – General Thinking Order (方法论笔记)

## 核心原则

算法设计不是“选一个范式”，而是一个 **逐步排除不适合范式的过程**。

> 真正成熟的算法思维 =  
> 不断尝试 → 发现结构不匹配 → 抽象失败原因 → 升级模型

---

## 一、动态规划（Dynamic Programming）——优先级最高

### 什么时候优先考虑 DP
- 问题具有：
  - 最优子结构（Optimal Substructure）
  - 无后效性（No Aftereffect）
- 子问题规模可以自然递减
- 状态可以被“局部化”并组合

### DP 的本质
> 用“状态最优”来表达“全局最优”

一旦成立：
- 正确性清晰
- 解是精确的
- 易于形式化证明

### 典型失败信号
- 决策之间存在强全局耦合
- 早期选择会不可逆地影响后续结构
- 状态空间无法被合理切分

📌 示例：Maximum Flow  
→ 流量分配是全局耦合的，不具备自然 DP 结构

---

## 二、贪心算法（Greedy）——不是解法，而是探测器

### 为什么 DP 失败后一定试 Greedy
- Greedy 是最简单的“局部最优假设”
- 失败本身就包含结构信息

### 贪心的真实作用
> Greedy ≠ 最终算法  
> Greedy = 用来暴露问题结构的工具

通过贪心可以发现：
- 局部最优何时破坏全局
- 失败是否可被修正
- 是否需要“撤销 / 回退”机制

📌 Maximum Flow 中：
- “找一条还能走的路径，尽量多送流”
- 失败 → 发现需要回退
- → 引出 **Residual Graph（残量网络）**

---

## 三、分治 / 搜索（Divide & Conquer / Search）

### 适用场景
- 问题可拆，但：
  - 子问题边界不稳定
  - 或需要枚举结构
- DP 状态爆炸或不可定义

### 常见形式
- Divide & Conquer
- DFS / BFS
- Backtracking + Pruning
- State Space Search

这一层通常是：
> “结构还在，但无法压缩为 DP 表”

---

## 四、图模型（Graph Modeling）——认知跃迁点

### 什么时候应该主动转成图
- 决策对象是“关系”而不是数值
- 约束来自：
  - 连通性
  - 冲突
  - 路径
  - 资源流动

### 图模型的优势
- 把“复杂约束”变成结构问题
- 全局性质（可达性、割、匹配）变得可计算

📌 很多问题的关键不是“算”，而是“建模”

---

## 五、流 / 匹配 / 割 —— 图中的结构级算法

### 这一层的特点
- 不像 DP
- 不像 Greedy
- 而是维护 **结构不变量**

常见问题：
- Maximum Flow
- Minimum Cut
- Bipartite Matching
- Circulation with constraints

> 解不是一步步算出来的  
> 而是通过“不断修正结构”逼近的

---

## 六、数学建模（LP / ILP / 对偶）

### 当组合思路不够时
- 将问题形式化为：
  - Linear Programming
  - Integer Linear Programming
- 利用：
  - 松弛
  - 对偶
  - 整数解性质

📌 许多经典算法（如最大流）
- 本质是 LP 的整数解
- 但用组合算法实现得更高效

---

## 七、近似 / 随机化 / 启发式（现实妥协层）

### 使用前提
- 问题 NP-hard
- 数据规模巨大
- 工程时间受限

### 常见方法
- Approximation Algorithms
- Randomized Algorithms
- Heuristics
- Metaheuristics（GA / SA / RL）

⚠️ 这是“现实约束层”，不是“理论失败”

---

## 总结性原则（非常重要）

1. 能 DP → 不要用别的
2. DP 不自然 → 用 Greedy 试探
3. Greedy 失败 → 分析失败结构
4. 失败结构 → 抽象为图
5. 图 → 流 / 匹配 / 割
6. 再不行 → 数学建模或近似

> 算法设计的本质不是“聪明”，  
> 而是 **对问题结构的诚实回应**

